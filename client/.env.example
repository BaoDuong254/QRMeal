NODE_ENV=development

# Frontend URL - exposed to client
NEXT_PUBLIC_URL=http://localhost:4007

# API Endpoint - use proxy to hide backend
# Change from: http://localhost:4008 (exposes backend)
# To: /api/proxy (hides backend via proxy)
NEXT_PUBLIC_API_ENDPOINT=/api/proxy
# Socket.IO Endpoint - use proxy to hide backend
NEXT_PUBLIC_SOCKET_URL=http://localhost:4007

# Backend URL - server-side only, NOT exposed to client
# In Docker: http://qrmeal-server:4008
# In local development: http://localhost:4008
BACKEND_URL=http://localhost:4008

# Google OAuth
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your-google-client-id
NEXT_PUBLIC_GOOGLE_AUTHORIZED_REDIRECT_URI=http://localhost:4007/auth/login/google
